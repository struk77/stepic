server {

    listen 80 default_server;
    root /home/box/web;

    location ^~ /uploads/ {
	root /home/box/web/uploads;
    }

    location ~* ^.+\.*$  {
	root /home/box/web/public;
    }

    location @extensionless {
        return 404;
    }

}

